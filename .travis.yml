language: cpp
sudo: false
matrix:
  include:
      os: linux
      compiler: g++
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - boost-latest
          packages:
            - gcc-5
            - g++-5
            - flex
            - bison
            - libboost1.55-all-dev
            - cmake
      env:   
        global:
          - COMPILER=g++-5
script:
  - ./script/build.sh -debug && cd ./build && make